  name: Deploy to Namecheap          ← 0 spaces (starts at beginning)

  on:                                ← 0 spaces
    push:                            ← 2 spaces
      branches: [ main ]             ← 4 spaces

  jobs:                              ← 0 spaces
    deploy:                          ← 2 spaces
      runs-on: ubuntu-latest         ← 4 spaces

      steps:                         ← 4 spaces
      - uses: actions/checkout@v2    ← 4 spaces (dash counts as part of
  spacing)

      - name: Deploy to Namecheap via FTP   ← 4 spaces
        uses: SamKirkland/FTP-Deploy-Action@4.3.3   ← 6 spaces
        with:                        ← 6 spaces
          server: ${{ secrets.FTP_SERVER }}          ← 8 spaces
          username: ${{ secrets.FTP_USERNAME }}      ← 8 spaces
